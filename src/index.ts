import {
    openLink,
    create,
    open,
    dismissLink,
    usePlaidEmitter,
    PlaidLink,
} from './PlaidLink';

export * from './Types';

export default PlaidLink;

export {
    PlaidLink,
    openLink,
    create,
    open,
    dismissLink,
    usePlaidEmitter,
};

// Components

export { EmbeddedLinkView } from './EmbeddedLink/EmbeddedLinkView';
